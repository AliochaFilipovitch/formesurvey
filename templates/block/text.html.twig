<div class="form-group">
	<textarea class="form-control" id="{{ question.id }}" placeholder="Votre réponse..."></textarea>
	<a href="{{ path('postAnswer', {'id': question.id, 'value': 'value'}) }}" id="{{ question.id }}" class="js-text btn btn-success btn-sm btn-block mt-1">
		<i class="far fa-paper-plane"></i> Enregistrer ma réponse
	</a>
</div>
<div class="alert {{ question.id }} alert-success m-1" role="alert">
</div>
